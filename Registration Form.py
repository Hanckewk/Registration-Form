import kivy
from kivy.app import App
from kivy.uix.boxlayout import Boxlayout

class RegistrationApp(App):
    def (self):
        self.title = "Registration Form"
        layout = Boxlayout(orientation = 'vertical', badding = 30, spacing=10)

        return layout

